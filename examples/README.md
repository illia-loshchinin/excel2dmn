# Examples

- `shipping_rates_DMN.xlsx` — a worked 4-row-header decision table.
- `SHIPPING_RATES.dmn` — the DMN 1.3 file produced by `excel2dmn shipping_rates_DMN.xlsx`.

Regenerate:

```bash
excel2dmn examples/shipping_rates_DMN.xlsx --out-dir examples --analyze
```
